export enum ROUTE {
  ORDERS = "/orders",
  GROUPS = "/groups",
  PRODUCTS = "/products",
  USERS = "/users",
  SETTINGS = "/settings",
}

export enum CATEGORY {
  ORDERS = "Приходы",
  GROUPS = "Группы",
  PRODUCTS = "Продукты",
  USERS = "Пользователи",
  SETTINGS = "Настройки",
}
